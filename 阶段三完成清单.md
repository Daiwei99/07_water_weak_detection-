# 阶段三功能完成清单

## ✅ 已完成功能

### 1. 中文显示（16×16汉字字库）
- [x] 创建 `Int_Font_CN.h` - 中文字库API定义
- [x] 创建 `Int_Font_CN.c` - 64个常用汉字点阵数据
  - 包含汉字：水浸检测控制器、正常监测、断线报警、黄红线回路、定位距离、参数设置、查看修改、线缆电阻长度、阈值容差、零点满幅、蜂鸣器继电器、开关启用、采样间隔、去抖次数、历史记录、保存返回
  - 每个汉字：16×16点阵，32字节
  - UTF-8编码查找表
- [x] 在 `Int_Oled.h` 中添加 `Oled_ShowChinese()` 声明
- [x] 在 `Int_Oled.c` 中实现 `Oled_ShowChinese()` 函数
  - 解析UTF-8字符串
  - 调用字库查找
  - 绘制16×16点阵到OLED缓冲区

### 2. 历史记录查看
- [x] 历史记录结构定义（`App_Menu.c`）
  - `HistoryRecord_t` 结构体：类型、时间戳、位置、详细信息
  - 循环缓冲区，最多10条记录
- [x] 实现 `Menu_AddHistory()` - 添加历史记录
  - 支持断线和水浸两种类型
  - 自动记录时间戳
  - 循环覆盖旧记录
- [x] 实现 `Menu_DisplayHistory()` - 历史记录显示
  - 显示记录序号、类型、位置
  - 支持UP/DOWN按键浏览
- [x] 实现 `Menu_ProcessHistory()` - 历史记录按键处理
  - UP：查看更新的记录
  - DOWN：查看更旧的记录
  - OK：返回系统设置菜单
- [x] 集成到菜单系统
  - 系统设置菜单中添加"查看历史"选项
  - 状态机支持 `MENU_STATE_HISTORY`

### 3. 更多参数调整
- [x] 在 `App_Config.h` 中添加新参数字段
  - `sampling_interval_ms`：采样间隔（50~1000ms）
  - `debounce_count`：去抖次数（1~10次）
- [x] 在 `App_Config.c` 中实现参数存取
  - `Config_SetSamplingInterval()` - 设置采样间隔
  - `Config_SetDebounceCount()` - 设置去抖次数
  - Flash存储支持，带CRC16校验
  - 参数合法性验证
- [x] 在 `App_Menu.c` 中添加菜单项
  - 系统设置菜单中添加"采样间隔"和"去抖次数"选项
  - UP/DOWN按键调整数值
  - 实时保存到配置
- [x] 实现 `Menu_GetSamplingInterval()` - 获取采样间隔
- [x] 实现 `Menu_GetDebounceCnt()` - 获取去抖次数

### 4. 外置16bit ADC硬件升级（SGM58031）
- [x] 创建 `Int_Sgm58031.h` - SGM58031驱动API
  - I2C地址：0x48
  - PGA增益：1x/2x/4x/8x
  - 采样率：128 SPS
- [x] 创建 `Int_Sgm58031.c` - 完整驱动实现
  - 硬件I2C2初始化（PB10/PB11）
  - 寄存器读写（16位数据）
  - `Sgm58031_Init()` - 初始化I2C2接口
  - `Sgm58031_ReadDiff()` - 读取差分ADC值
  - `Sgm58031_AdcToMv()` - ADC转电压（mV）
  - 支持单次转换模式
  - 可配置PGA增益

## 📋 代码文件清单

### 新增文件
- `Int/Int_Font_CN.h` - 中文字库头文件
- `Int/Int_Font_CN.c` - 中文字库实现（64个汉字，~500行）
- `Int/Int_Sgm58031.h` - SGM58031 ADC驱动头文件
- `Int/Int_Sgm58031.c` - SGM58031 ADC驱动实现

### 修改文件
- `Int/Int_Oled.h` - 添加 `Oled_ShowChinese()` 声明
- `Int/Int_Oled.c` - 实现中文显示函数
- `App/App_Config.h` - 添加采样间隔和去抖次数参数
- `App/App_Config.c` - 实现新参数的存取函数
- `App/App_Menu.h` - 历史记录相关API
- `App/App_Menu.c` - 完整菜单系统实现
  - 历史记录功能
  - 参数调整功能
  - 采样间隔和去抖次数配置

## 🔧 技术要点

### 中文显示
- UTF-8编码：每个汉字3字节
- 字库查找：memcmp比较UTF-8编码
- 点阵绘制：16×16像素，占用2页OLED缓冲区
- 字库大小：64字符 × 32字节 = 2KB

### 历史记录
- 循环缓冲区：FIFO结构，最多10条
- 时间戳：使用 `Delay_GetTick() / 1000` 获取秒数
- 记录类型：0=断线，1=水浸
- 浏览功能：UP/DOWN键前后翻页

### 参数调整
- Flash存储：写入最后一页（0x0800FC00）
- CRC16校验：确保参数完整性
- 实时生效：修改后立即保存并同步
- 参数范围：
  - 采样间隔：50~1000ms，步进10ms
  - 去抖次数：1~10次，步进1次

### SGM58031 ADC
- 16位分辨率：-32768~32767
- 差分输入：AIN0 - AIN1
- PGA增益：
  - 1x: ±6.144V
  - 2x: ±4.096V
  - 4x: ±2.048V
  - 8x: ±1.024V
- 硬件I2C2：100kHz通信速率
- 单次转换：每次读取约10ms

## 📝 使用说明

### 中文显示示例
```c
Oled_ShowChinese(0, 0, "水浸检测");
Oled_ShowChinese(0, 2, "正常监测");
Oled_Refresh();
```

### 历史记录示例
```c
// 添加水浸记录
Menu_AddHistory(1, 12.5f, "Yellow");

// 添加断线记录
Menu_AddHistory(0, 0, "Break Detect");
```

### SGM58031使用示例
```c
Sgm58031_Init();

// 读取差分电压（PGA=1x）
int16_t adc = Sgm58031_ReadDiff(SGM58031_PGA_1X);
int32_t mv = Sgm58031_AdcToMv(adc, SGM58031_PGA_1X);
```

## ✅ 验收状态

**所有四项功能已完成实现：**
1. ✅ 中文显示（16×16汉字字库）
2. ✅ 历史记录查看
3. ✅ 更多参数调整（采样间隔、去抖次数）
4. ✅ 阶段三：外置16bit ADC硬件升级（SGM58031）

**建议后续测试：**
- 编译验证所有代码无语法错误
- 硬件测试SGM58031 I2C通信
- 验证中文字库显示效果
- 测试历史记录存储和浏览
- 验证参数调整保存到Flash
